import React, { Component } from 'react'

class Dropdown extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <select className="c-dropdown">

      </select>
    )
  }
}

export default Dropdown
